export default function SubmitButton() {
    return (
        <div className="p-4 bg-[var(--white)] flex justify-end text-sm">
            <button className="bg-[var(--blue-950)] text-[var(--white)] py-2 px-4 rounded-sm">
                Next Step
            </button>
        </div>

    );
}

